spring:
  application:
    name: mall-search
  profiles:
    active: dev #default to be dev

server:
  port: 8081

mybatis:
  mapper-locations:
    - classpath:dao/*.xml
    - classpath*:com/**/mapper/*.xml